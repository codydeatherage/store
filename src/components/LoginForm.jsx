import styled from 'styled-components'

const FormBody = styled.div.attr({
    className: 'card card-body'
})`
    border-radius :10%;
    width: 40vw;
    height: 70vh;
`

const LoginForm = () => {
    return (
        <FormBody>
            
        </FormBody>
    )
}

export default LoginForm